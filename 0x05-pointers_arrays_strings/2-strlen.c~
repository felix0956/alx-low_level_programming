/**
 * _strlen - function that returns the length of a string.
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
  int i;
  int count;
  for (i = 0; s[i] <= '\0'; i++)
    count += i
  return (count);
}

